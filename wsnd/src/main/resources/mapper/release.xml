<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lxy.oa.dao.ReleaseDao">
    <insert id="insertrelease" parameterType="Map">
        INSERT INTO sys_release (gameid,title,content,photo,release_time,user_id) VALUES (#{gameid},#{title},#{content},#{photo},#{releasetime},#{userid})
    </insert>

</mapper>